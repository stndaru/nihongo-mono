/**
 * The fixed Drive names, shared by the REST layer and the UI (which shows
 * users where their data lives without importing the lazy engine graph).
 */
export const FOLDER_NAME = 'Nihongo Mono'
export const FILE_NAME = 'progress.json'
